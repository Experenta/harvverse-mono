// Server-side auth helper. Client components use useCurrentUser() from hooks/use-auth.ts.
export function getAuthUser() {
  // TODO: Replace with Clerk's auth() helper when server components need the current user
  return null;
}
